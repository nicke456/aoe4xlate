websockets>=12.0
keyboard>=0.13.5
deep-translator>=1.11.4
requests>=2.31.0
pyyaml>=6.0
# pywebview dropped: WebView2 uses DirectComposition which ignores SetLayeredWindowAttributes,
# making color-key/alpha transparency unreliable. tkinter (stdlib) works reliably via GDI.
